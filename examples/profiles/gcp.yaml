# Synthetic template for managed services; no Kubernetes cluster is required.
profile_version: 1
profile: example-profile
provider: gcp
environments:
  staging:
    gcp_project: example-staging-project
    location: europe-west2
